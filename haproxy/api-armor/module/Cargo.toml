[package]
name = "haproxy-api-armor-module"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua = { workspace = true }
api-armor-body-parser = { path = "../body_parser" }
api-armor-jwt-validator = { path = "../jwt_validator" }
