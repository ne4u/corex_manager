[package]
name = "haproxy-disk-cache-module"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
haproxy-api = { version = "0.9" }
haproxy-disk-cache = { path = ".." }
mlua = "0.11"
