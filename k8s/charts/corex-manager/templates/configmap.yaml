apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ include "corex-manager.namespace" . }}
  labels:
    {{- include "corex-manager.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
  COREX_RUNTIME: "kubernetes"
  HAPROXY_CONTAINER_NAME: "corex"
  VARNISH_CONTAINER_NAME: "varnish"
  VECTOR_CONTAINER_NAME: "vector"
  K8S_VECTOR_CONTAINER: "vector"
  VECTOR_CONFIG_PATH: "/app/data/vector/vector.toml"
  # Vector is a sidecar in the same pod — HAProxy logs to localhost.
  VECTOR_SYSLOG_TARGET: "127.0.0.1:601"
  CORAZA_SPOA_HOST: "127.0.0.1"
  VARNISH_PORT: "6081"
  DATAPLANE_API_URL: "https://127.0.0.1:5555/v3"
  ACME_CHALLENGE_BACKEND_HOST: "127.0.0.1"
  ACME_CHALLENGE_BACKEND_PORT: "80"
  MCP_GATEWAY_RS_INTERNAL_HOST: "{{ if .Values.mcpGateway.enabled }}127.0.0.1{{ end }}"
  MCP_GATEWAY_RS_INTERNAL_PORT: "{{ if .Values.mcpGateway.enabled }}8089{{ end }}"
  MCP_SERVER_INTERNAL_HOST: "{{ if .Values.mcpServer.enabled }}127.0.0.1{{ end }}"
